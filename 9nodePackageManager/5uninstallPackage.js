/**
 * Uninstall Package
 * - Untuk uninstall package 
 *      + Jika package berada di object dependencies
 *          npm uninstall <package-name>
 *      + Jika package berada di devDependencies
 *          npm uninstall <package-name> --save-dev
 * 
 */